<%= form_with(model: personal_feature, local: true) do |form| %>
  <% if personal_feature.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(personal_feature.errors.count, "error") %> prohibited this personal_feature from being saved:</h2>

      <ul>
        <% personal_feature.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
